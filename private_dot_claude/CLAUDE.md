# Global Claude Code Instructions

## Time Format

Always use **local time** for all timestamps. Use the following command:

```bash
date +"%Y-%m-%dT%H:%M:%S%z"
```

Do NOT use UTC time (`date -u`) unless explicitly requested by the user.
